/**
 * The class on a description editor's text, and what description.css styles.
 * Its own module so the Item's form can tell a drop into the text from one
 * anywhere else without importing the editor, which is a lazy chunk.
 */
export const DESCRIPTION_TEXT_CLASS = 'description-prose';
