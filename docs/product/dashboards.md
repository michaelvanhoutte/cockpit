# Dashboards

**How an Item gets onto a Panel is shown until it has been done once**, at both ends of the gesture: while nothing in a Workspace has been filed, an empty Panel says how one arrives and the Inbox says it from the other side once it holds anything. Both name the row's own menu as well as the drag, because there is no drag from the Inbox to a Panel on a phone. The first filing anywhere in that Workspace ends both, the gesture having been done rather than read about.

**Dashboard** — a named view *inside* a Workspace, switched between like tabs. The bar under the workspace tabs is where they live ("Add and switch dashboards", issue 32): a `+` after them adds one, and each tab carries a menu of what can be done to it. **The Inbox is not a Dashboard**; it sits beside them (see "The Inbox and the triage flow" in `docs/product/inbox.md`). Every Workspace has at least one, and a Dashboard's name is unique within its Workspace, so two Workspaces may each have a *Research*. (Earlier drafts called this level a *Page*; "dashboard" is what the product is called and what everyone reaches for, so "page" goes back to meaning an ordinary screen.)

**A Dashboard is changed on its own tab**, exactly as a Workspace is and opened the same three ways ("Change a workspace or a dashboard on the tab it is", issue 267): *Edit…* and *Delete*, on the tab that already names it. It was a list behind the bar's own menu at the far right, and that menu held nothing else, so both are gone. The form opens over the Workspace rather than replacing it, because renaming a Dashboard is a detour from working on one: the panels, the Inbox and the bar stay behind it, closing puts you back with nothing to reload, and there is no address for it. Adding is not on the tab — the `+` is a one-gesture thing you do often, and belongs on the bar the new tab will appear on. **What it opens is the same question a Panel is named in, and it says what a Dashboard is**: pressing `+` is the moment somebody is asking, and that is the moment worth answering in. It grew a field at the end of the bar until then, which is a strip with room for a box and a button and nothing else. The Inbox has no such menu, being no Dashboard of the Workspace. Renaming obeys the rules adding does, so *Research* may become *RESEARCH* while a name another Dashboard of that Workspace holds is refused with a message saying which. Deleting asks first in the same dialog as everywhere else and says what the Dashboard takes with it: how many Panels go with it, or that there is nothing on it. **A Workspace's last Dashboard cannot be deleted**, the one place the app refuses to delete something: the last Workspace may go because the app can offer to make one, while a Workspace with no Dashboard has no view at all. The entry is in the menu, unavailable, with that sentence on it, rather than offered and then refused. Deleting the one you were looking at moves the Workspace on behind it, its own address deciding where you land.

**Panel** — a movable, titled box on a Dashboard, sharing a row with up to three others, each a **saved, filtered view of items**, with a user-editable title.

Panels are added, renamed, moved and deleted **on the Dashboard itself** ("Panels on a dashboard, with per-screen-size layouts", issue 33), not on a settings page, because dragging one Panel past another *is* the editing and has to happen where the Panels are drawn. **Add a Panel from the control at the right of the Dashboard bar** ("Layouts: the arrangement that follows your screen"), which is where the arrangement it joins is also chosen and which asks what to call it in the same question a Dashboard is named in, saying what a Panel is; each Panel carries a menu opened the way a Dashboard's or a Workspace's own tab opens its (see "One menu, however it opens" in `docs/functional-definition.md`), offering rename, move and delete. Moving is in both the menu and the pointer, because a drag is unreachable from a keyboard and absent on a touchscreen: drag a Panel's header onto another Panel to join its row, or into the gap between two rows to take a row of its own. **The Panels move as the drag does**, so the arrangement under the hand is the one dropping will keep and there is nothing to read off an indicator — the Panel in the air is drawn back in its slot, the rest step aside as it passes, and a placement that cannot happen, a row already four across, shows nothing happening rather than promising a move the drop would decline. Letting go keeps what is drawn; a drag that ends where it started changes nothing, and one the browser takes back for a scroll puts the Panels where they were. Meanwhile **Move left** / **Move right** — **Move up** / **Move down** where the Panel has its row to itself — walk it a step at a time along its row and off either end onto a line of its own. **Joining a row is the one move only the pointer has**, which is a gap rather than a decision: the menu can take a Panel off a row but has no entry that puts one beside another. **A Panel still has no size of its own — it fills its share of a row, and the row is what carries a height — but both are set by dragging the lines the row is drawn with** ("Drag a row taller and a panel wider, on the dashboard itself", issue 255). The line **under a row** sets how tall it is, between 160 and 720 pixels so a row can neither swallow the screen nor shrink below a list worth reading, and double-clicking it puts the row back to being as tall as what is in it. The line **between two Panels** moves whole twelfths from one to the other, so the row still adds up to a whole and the Panels beside the pair keep their share. Neither line is drawn at rest, one appearing under the pointer, because a divider between every pair of Panels would be chrome charged to every Dashboard for a rare gesture. A size goes into the Layout on screen exactly as a move does — drawn as the hand goes, kept when it stops — so a row sized on *Wide* is untouched on *Phone*. Both lines are the pointer's, so sizing is absent from a keyboard and a touchscreen until the entries that step them land ("Move and resize a panel with the cursor keys, from two entries instead of four", issue 256); and a row still evens out when a Panel joins or leaves it, so shares set here are given up when the row's membership changes. A title is required, stored trimmed, one line of at most 60 characters, and unique among a Dashboard's live Panels whatever the capitalization, so two Dashboards of one Workspace may each have a *Reading list*. Deleting asks first, in the same dialog as everywhere else, and says the Panel goes from every Layout of its Dashboard. **Every Dashboard arrives with one, called *Panel 1***, so a Workspace has somewhere to file an Item into from the moment it exists — the Inbox holds what no Panel holds, so a Dashboard with no Panels is an Inbox with no way out of it. That is a starting condition and not a rule: a Dashboard may end up with no Panels at all, unlike a Workspace, whose last Dashboard cannot be deleted. **A Panel holds the Items filed into it**, in an order you set, and drawing them is all it does: a rule for what *arrives* in one without being filed is configuration it does not have yet ("Panel configuration: connections and free-text description", issue 35).

**Layout** — one named arrangement of a Dashboard's Panels, remembering the screen width it was made at, so the same Dashboard reads well on a phone and on a 4K screen. A Dashboard may have several, and which one it is drawn with is decided in "Layouts: the arrangement that follows your screen".

## 6. Dashboards and Panels

**Panels** behave like the tiles on Azumuta's Analytics dashboards: each sits on a row, **drag to move**, an editable **title**, and a **menu of its own** (configure, rename, remove), opened the same way a Dashboard's or a Workspace's own tab opens its (see "One menu, however it opens" in `docs/functional-definition.md`). **Adding a Panel is a control on the Dashboard's own bar** ("Layouts: the arrangement that follows your screen"), beside the one naming the arrangement the new Panel joins. Rows reflow on smaller screens so the same Dashboard works on phone and desktop.

**A Dashboard is an ordered list of rows.** Each holds up to four Panels, which fill its width between them and share its height; a Panel's width is its share of its row rather than a measurement, so the Dashboard never scrolls sideways on any device — a Layout made for a 4K screen and opened on a laptop keeps each share and squeezes what it measures, while the text keeps its normal size. **Nothing wraps**, which is the whole of why this shape replaced a grid: which Panels share a line is something a person put there, not a consequence of their widths, so it survives the screen changing under it. Moving a Panel is therefore a drop — onto another Panel to join its row, or into a gap to take a row of its own — rather than a move to a coordinate, and there are no holes to leave behind.

**A Panel holds Items or text**, chosen when the Panel is made and never after ("Put a panel of text on a dashboard, and write in it", issue 250). Everything else in this section is about a Panel of Items. A Panel of **text** holds prose written straight onto the Dashboard — a standing agenda, a note the team reads, a heading over a row — and its own menu settles two things about it. It is **read-only** or open for writing; and its words are drawn as the **characters that were typed** or as **what they mean** ("Format what a panel says, without making every dashboard pay for an editor", issue 251). Neither is a conversion: the same Markdown is stored either way, so switching back and forth loses nothing. **Characters is the default**, because drawing them costs nothing to fetch, where formatting needs a renderer and writing formatted text needs an editor — so a Dashboard costs nothing to open until somebody asks. Read-only is what a Panel of text settles into, being a thing to read; only the Panel you have just made arrives open, because an empty box refusing to be written in is nonsense one menu away from the gesture that made it. **Nothing is filed onto a Panel of text**: it is not offered when filing, it takes no drop, and an Item asked onto it directly is refused — one filed there would leave the Inbox and be drawn nowhere.

**Panel types for v1**, all of them Panels of Items:

- **Project** — all Items associated with a Project, across all sources.
- **Person** — Items and notes tied to a person; doubles as "things to discuss with them".
- **Topic/Area** — a free-form bucket such as *Research*.
- **Focus** — Items flagged Today / This Week / This Month / This Quarter (see "Focus and Goals" in `docs/product/items.md`).
- **Highlights / AI summary** — an auto-generated digest (see "AI layer" in `docs/product/items.md`).
- **Payments due** *(iteration 2)* — every open Item classified as requiring a payment, across the Workspace's mail accounts, showing extracted amount and due date, using the standard deadline colors (see "Deadline color states" in `docs/product/items.md`) and leaving once marked paid. Classification and extraction are AI work on ingest (see "AI layer" in `docs/product/items.md`); detecting "paid" is open decision #16. Functionally a specialized live-rule Panel ("What a Panel shows", below), but its rule is an AI classification rather than a source filter.
- **Reading digest** *(iteration 2)* — highlights extracted from technology and research email instead of the emails themselves: the AI detects that a mail is content, splits out its individual stories, matches them against topics of interest, and presents a short "worth reading today" selection linking to the source. This mail never hits To Process and carries no obligation — what is unread ages out rather than accumulating (problem 8). Highlights says what needs follow-up; the Reading digest says what might be worth reading.
- **Kanban** *(candidate for v1 or fast-follow)* — Items as cards in columns you define.
- **Calendar/agenda** *(later)*.

A **Dashboard** is a saved arrangement of Panels; there can be as many per Workspace as you like.

### 6.1 How Items render in a Panel — action cards

Any Panel can present its Items as an action list rather than raw message previews — a **general Panel capability**, not tied to one use case. Each card has:

- A **next-action label** — a short distilled summary of what really needs doing (*"Answer pricing question"*, *"Reply to Tom's question on Part 11 compatibility"*). The required action usually is *not* the last message, so this is genuine LLM work (action extraction — see "AI layer" in `docs/product/items.md`), not a copy of the subject line. For internal notes the line is yours, and either way it is **always editable**.
- A **source icon** — mail, Slack, Notion, or an internal-note marker.
- **Deadline color** (see "Deadline color states" in `docs/product/items.md`) and a **priority** highlight, independent of any deadline.

Panel-level controls, supported by every Panel: **manual sort** (drag to reorder, remembered); **grouping** by any field (person, project, type, priority); **highlighting** by priority and deadline.

**Where a card links.** An Item from a connected source deep-links into that app at exactly the right place (the Gmail thread, the Slack message, the Notion page), so you act in the real tool rather than a degraded copy. An internal note opens inside this app for viewing and editing.

**The round-trip — updating the card after you replied elsewhere.** The outcome is not always "done": a reply that fully resolves it marks **Done** and the card leaves the Panel; a reply that leaves you awaiting an answer moves the Item to **Waiting / Follow-up**, keeps it in the Panel, and **rewrites its next-action label** — *"Reply to Tom's question on Part 11"* becomes *"Follow up: awaiting Tom's answer on Part 11."* So each Item carries a state-dependent *current next-action*. How the app learns you replied is open decision #10: detect the outbound reply via two-way sync, or prompt on return ("Handled? → Done / Waiting / Still to do"). Recommended: prompt on return in v1.

### 6.2 What a Panel shows — manual promotion + live rules

Two mechanisms combine:

- **Filing.** Any Item can be filed onto a Panel from its own menu, which is what takes it out of the Inbox ("The Inbox and the triage flow"). An Item filed on several Panels appears on all of them: **Add to…** in a row's menu shows it on one more, and **Remove from this panel** stops one Panel showing it while every other carries on — an Item removed from the only Panel holding it is back in the Inbox. Dragging a row from one Panel onto another **asks which was meant**, because both are reasonable and a drag says neither ("Ask whether to move an item to a panel or add it to one", issue 142); a drop from the Inbox asks nothing, there being no answer that leaves it there. On a screen with a pointer it is also **dragged** into the Panel, and dropped between the rows already there: where it is let go is where it lands, and dropping it back into the Panel it is already on reorders that Panel ("Drag an item into a panel, and drop it where you want it", issue 141). A drag is unreachable from a keyboard and absent on a touchscreen, so a row's own menu carries **Move up** and **Move down** — Ordering, the same move made two ways. **A drag near an edge scrolls the page**, so a Panel below the fold can be aimed at — which the browser does by itself and Cockpit does not implement — and **resting one on another Dashboard's name switches to it**, so a Panel that was not on screen when the drag began can be dropped on — and that Dashboard is the one still open afterwards, because it is where you just put something ("Scroll while dragging, and switch dashboards by resting on one", issue 143).
- **Live rules (saved queries).** A Panel can be configured with a rule for what belongs in it ("all messages from the **cust-AtlasCopco** Slack channel", "emails labelled *Pricing*", "Items associated with Project Falcon"). The Panel remembers the rule and any future match **appears automatically**, without touching the Inbox — the panel-level expression of the direct-routing model in "How an Item reaches a box" (`docs/product/inbox.md`).

**Rules are configured in plain English, not through a wizard.** You describe what the Panel should show in a free-text sentence (*"all emails from customers"*, *"Slack messages where I'm mentioned in the customer channels"*) and the AI translates it into the underlying saved query (see "AI layer" in `docs/product/items.md`). The app plays the interpretation back in understandable terms ("this will show: emails, from senders matching your customer list, not yet done") so it can be confirmed or refined by editing the sentence. A multi-step rule wizard is explicitly not wanted; the structured query is the stored, inspectable result of the sentence.

**Attach-and-monitor scope prompt.** Adding a Slack item to a Panel asks *how much* to monitor: **this thread**, **this conversation/DM**, or **this channel** (the live-rule case). The same generalizes to other sources — one email thread versus a whole label, one Notion page versus a database. The scope picked becomes the Panel's live rule.

### 6.3 Layouts: the arrangement that follows your screen

A 4K screen fits more Panels side by side than a laptop, which fits more than a phone, so a Dashboard can hold **several Layouts**. Each one **has a name** and **records the screen width it was made at** ("Pick the layout you are on, by name"). That width is whatever the screen happened to be, not one of a fixed set of breakpoints, so there is nothing to belong to and nothing to configure; it is read only when a screen is matched to a Layout.

**A Layout is named, and the name is what you pick it by.** Required, stored trimmed, one line of at most 60 characters, and unique among its Dashboard's Layouts whatever the capitalization — the rules a Panel's title obeys, in the same scope, so two Dashboards of one Workspace may each have a *Wide*. Layouts used to be known by the width alone, and *Made for 1463 px* names a number a window is only accidentally.

**A Dashboard is drawn with the Layout nearest the screen in front of you**, a tie going to the narrower so two devices of the same size agree ("Layouts follow the screen you are on"). Moving a window to another monitor redraws it there, and a monitor no Layout was made for gets the nearest one rather than a Layout of its own — making one is always yours to do, never something that happens because a screen appeared.

**Which Layout a Dashboard is drawn with is a control at the right of its own bar**, beside the tab whose arrangement it names, along with the way to add a Panel. Both are drawn only where a Dashboard is open, since the bar is also the Inbox's. The control names the Layout in use; its menu lists each Layout by name with the width it was made at, and offers *New layout from this one*, *Rename* and *Delete*. **Picking one by name overrides the screen only while you are on that screen**: the pick lasts as long as the nearest Layout would not have changed anyway, so resizing a window or opening the devtools keeps it and moving to another monitor drops it. Making a Layout puts you on it the same way, on the screen you made it for. There was an *Automatic* entry above the Layouts, and it was one concept too many — a row that was not a Layout, a badge to say which of two ways you were on one, and every route out of it a gesture nobody read as leaving it, making a Layout most of all. Picks are remembered in the browser rather than stored, since the whole point of Layouts is that the phone and the desktop want different ones. A Layout that is deleted, or a picked one that another device deletes, simply falls back to the nearest remaining.

**Changing the arrangement changes the Layout you are on, and asks nothing.** Dragging and moving from a Panel's own menu both go into it, whatever screen you are on. There used to be a question here — *change this Layout*, or *make a Layout for this screen* — and it existed because the Layout had been picked *for* you and either answer was reasonable. Picking it by name is what makes the gesture mean what it says.

**A Dashboard that has never been arranged is drawn to fit the screen it is on**: the Panels in the order they were added, on rows of a count that keeps a Panel worth reading — one across on a phone, three on a laptop, four on anything wider. It has no Layout at all, and the first move records one, named for the size of screen it was made on — *Phone*, *Tablet*, *Laptop* or *Wide*, and yours to change. **A Dashboard that has a Layout keeps one**, so deleting the last is refused with that sentence on the entry: what it protects is an arrangement somebody made, not the existence of a row.
